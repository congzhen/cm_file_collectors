export enum EresDetatilsType {
    show,
    play,
}

export enum EresDramaSeriesType {
    movies = 'movies',
    comic = 'comic',
    atlas = 'atlas',
    files = 'files',
    videoLink = 'videoLink',
}

export enum EresUpdate {
    updateData,
    updataDetailsView,
}

export enum EsearchLogic {
    single,
    or,
    and,
    not
}
